<div id="<%= dom_id note %>">
  <p>
    <strong>Title:</strong>
    <%= note.title %>
  </p>

  <% if user_signed_in? %>
    <p>
      <strong>Author:</strong>
      <%= User.find(note.user_id).username %>
    </p>
  <% end %>

</div>